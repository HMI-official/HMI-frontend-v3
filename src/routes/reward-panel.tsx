import React from "react";
import styled from "styled-components";

const RewardPanel = () => {
  return <Section>this is reward panel</Section>;
};

export default RewardPanel;

const Section = styled.section`
  width: 100vw;
  height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
`;
